<%= form_for(@game_instance) do |f| %>
  <% if @game_instance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_instance.errors.count, "error") %> prohibited this game_instance from being saved:</h2>

      <ul>
      <% @game_instance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
